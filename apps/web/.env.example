# Hivemind Web App Environment Variables
# Copy this file to .env.local and fill in your values

# Optional: Analytics (if implemented)
# NEXT_PUBLIC_ANALYTICS_ID=

# Optional: Custom API endpoints (for enterprise/self-hosted)
# NEXT_PUBLIC_OPENAI_BASE_URL=https://api.openai.com/v1
# NEXT_PUBLIC_ANTHROPIC_BASE_URL=https://api.anthropic.com
# NEXT_PUBLIC_GOOGLE_BASE_URL=https://generativelanguage.googleapis.com

# Note: API keys are entered through the UI and stored encrypted in localStorage
# They are NOT set as environment variables for security reasons
